import GameTileConfig from "../../molecules/GameTile/GameTileConfig";

const GameListConfig = {
    imgHeight: GameTileConfig.height,
    imgWidth: GameTileConfig.width,
    navHeight: 36,
    navWidth: 36
};

export default GameListConfig;